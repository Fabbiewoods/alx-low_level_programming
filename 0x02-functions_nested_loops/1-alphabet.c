#include "main.h"

/**
 * print_alphabet - this function is to print alphabets in lower case.
 *
 * Return: this returns 0
 */

void print_alphabet(void)
{
	char u;

	for (u = 'a' ; u <= 'z' ; u++)
		_putchar(u);
	_putchar('\n');
}
