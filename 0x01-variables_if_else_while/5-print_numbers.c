#include <stdio.h>

/**
 * main - this is the entry point for the program.
 *
 * Return: this can be 0 on success.
 */

int main(void)
{
	int k;

	for (k = 0 ; k < 10 ; k++)
		printf("%d", k);
	printf("\n");
	return (0);
}
