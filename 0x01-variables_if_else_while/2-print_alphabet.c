#include <stdio.h>

/**
 * main - this is tge entry point of the program.
 *
 * Return: this can return 0 on success.
 */

int main(void)
{
	char i;

	i = 'a';
	while
		(i <= 'z') {
			putchar(i);
			i++;
		}
	putchar('\n');
	return (0);
}

